dataloader:
  data_name: cifar100
  batch_size: 256
teacher_model:
  model_name: resnet18
student_model:
  model_name: resnet18
optimizer:
  opt_name: sgd
  lr: 0.1
  momentum: 0.9
  weight_decay: 0.0005
trainer:
  epochs: 200
  seed: 42
logging:
  framework_name: wandb
  project_name: knowledge-distillation-cifar100